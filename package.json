{
  "name": "doc-flow",
  "version": "1.0.0",
  "description": "A Figma plugin for document content flow",
  "type": "module",
  "main": "dist/code.js",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "build": "vite build && vite build -c vite.ui.config.ts",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "fast-levenshtein": "^3.0.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.85.0",
    "@types/fast-levenshtein": "^0.0.4",
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  }
}